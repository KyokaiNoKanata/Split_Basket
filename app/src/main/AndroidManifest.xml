<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Android 13+ 通知权限声明 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Split_Basket">

        <activity android:name=".HomeActivity" android:exported="true" android:launchMode="singleTop"
            android:windowLayoutInDisplayCutoutMode="shortEdges">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InventoryActivity" android:launchMode="singleTop"
            android:windowLayoutInDisplayCutoutMode="shortEdges" />
        <activity android:name=".ListActivity" android:launchMode="singleTop"
            android:windowLayoutInDisplayCutoutMode="shortEdges" />
        <activity android:name=".BillActivity" android:launchMode="singleTop"
            android:windowLayoutInDisplayCutoutMode="shortEdges" />

        <activity android:name=".InventoryAddActivity" android:launchMode="singleTop"
            android:windowLayoutInDisplayCutoutMode="shortEdges" />

        <receiver android:name=".ReminderReceiver" android:exported="false"/>

        <!-- FileProvider for camera output -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>

